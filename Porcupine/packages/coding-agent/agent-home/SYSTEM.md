# SYSTEM.md (optional full replace)

This file **replaces** the entire default system prompt (including the built-in
tools list body). Prefer PROMPT.md + PERSONALITY.md for normal identity edits.

Only place a SYSTEM.md here if you intentionally want a full custom system prompt.
Copy from PROMPT.md as a starting point if needed.
